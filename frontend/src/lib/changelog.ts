/**
 * Returns the ID of the latest announcement shown in the What's New dialog.
 * Update this value whenever a new announcement is added to the changelog.
 */
export function getLatestAnnouncementId(): string | undefined {
  return undefined
}
